{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "phonebook backend",
  "license": "ISC",
  "author": "danish",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd frontend/phonebook && npm run build && cp -r dist ../../dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.12.1"
  }
}
